import 'package:flutter/material.dart' show BuildContext, Container, StatelessWidget, Text, Widget;

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('home'),
    );
  }
}